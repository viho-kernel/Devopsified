replicaCount: 2

image:
  repository: go-web-app
  tag: latest
  pullPolicy: IfNotPresent

deployment:
  port: 8080

service:
  type: ClusterIP
  port: 80
  targetPort: 8080

ingress:
  enabled: false