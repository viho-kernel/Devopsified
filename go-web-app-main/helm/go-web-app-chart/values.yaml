replicaCount: 2

image:
  repository: viharireddy18/go-web-app
  pullPolicy: IfNotPresent
  tag: "12345"

deployment:
  port: 8080

service:
  type: ClusterIP
  port: 80
  targetPort: 8080

ingress:
  enabled: false
